# Web app test locations

## Rules

- Playwright tests live only in `apps/web/e2e`.
- Vitest tests live only in `apps/web/src/**/__tests__/**`.
- Fixtures live next to their module in `__fixtures__`.
- Do not add tests next to production files.

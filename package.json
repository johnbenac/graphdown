{
  "name": "graphdown",
  "version": "0.0.0",
  "description": "Graphdown dataset validator.",
  "license": "MIT",
  "repository": "https://github.com/johnbenac/graphdown",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "graphdown": "dist/cli.js"
  },
  "files": [
    "dist/",
    "validateDataset.js",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "lint": "eslint \"src/**/*.ts\"",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "npm run build && node --test",
    "build": "tsc && node scripts/add-shebang.js"
  },
  "dependencies": {
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "@types/node": "^20.16.10",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.10.0"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  }
}

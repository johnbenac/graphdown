{
  "paths": [],
  "titles": [
    "accepts directory entries after validation",
    "buildZipBytesFromSnapshot excludes .git by default",
    "buildZipBytesFromSnapshot handles cross-realm Uint8Array values",
    "clears all values",
    "clears corrupt data when loading",
    "clears the active dataset",
    "clears the active dataset when snapshot or meta is missing",
    "collectStringValues collects nested strings",
    "collectStringValues collects nested strings from objects + arrays",
    "collectStringValues ignores non-strings safely",
    "datasets screen renders",
    "decodeUtf8Strict returns error for invalid utf8",
    "decodeUtf8Strict returns ok for valid utf8",
    "decodeUtf8StrictOrThrow throws for invalid utf8",
    "deletes values",
    "deletes values by key",
    "derives record identity from YAML instead of file paths",
    "does not clear the persisted dataset when runtime fails to open on loadActive",
    "does not expose persistence helpers",
    "does not throw on invalid inputs",
    "drops non-dataset files and records them as ignored",
    "encodes and decodes the same value",
    "encodes UTF-8 bytes",
    "exports GitHub import helpers",
    "exports selection helpers",
    "exports zip IO helpers",
    "handles cases with no warnings",
    "ignores non-semantic files that are not declared by plugins",
    "ignores plugin bundles when indexing records",
    "import screen renders",
    "imports a zip (with plugin bundles + binary + blocks), selects semantic files, validates, and opens runtime",
    "imports from a stubbed GitHub repo, validates, and opens runtime API",
    "imports GraphMD markdown files regardless of folder layout",
    "imports GraphMD markdown in non-canonical paths",
    "includes blocks and GraphMD markdown while sorting ignored paths",
    "includes blocks under blocks and reports ignored files",
    "includes entry, files, and binaryFiles with deterministic dedupe order",
    "includes plugin manifest and bundles via shared selection engine",
    "indexes outgoing/incoming links from record body + fields, and ignores plugin bundle files",
    "indexes parent/child/root relationships correctly",
    "is deterministic regardless of file iteration order",
    "isImportError returns false for non-import errors",
    "isImportError returns true for ImportError",
    "loadDatasetSnapshotFromZipBytes normalizes backslashes",
    "loadDatasetSnapshotFromZipBytes rejects .. paths",
    "loads graph-dataset without throwing",
    "loads plugin-valid-dataset and validates OK (plugins ignored for now)",
    "loads valid-dataset and validates OK",
    "normalizeLineEndings normalizes CRLF and CR to LF",
    "normalizes backslashes and dot segments",
    "preserves bytes and encodes strings in UTF-8",
    "preserves message on ImportError",
    "Record Link Graph exposes type and record lookup by identity",
    "rejects absolute paths",
    "rejects duplicate normalized paths",
    "rejects missing or invalid fields",
    "rejects parent cycles (dataset + runtime both surface E_PARENT_CYCLE)",
    "rejects traversal and empty segments",
    "renders navigation links",
    "renders the datasets route",
    "renders the export route",
    "renders the import route",
    "renders the type fields and body content",
    "reports duplicates when records collide",
    "reports missing plugin bundle paths without throwing",
    "returns an IndexedDbStore when IndexedDB is available",
    "returns errors for invalid payloads",
    "returns ignored paths in deterministic order",
    "returns null when no active dataset exists",
    "returns runtime + index on valid snapshot",
    "returns undefined for missing keys",
    "round-trips a valid dataset snapshot through zip bytes",
    "round-trips snapshot data with deterministic ordering",
    "round-trips values",
    "runtime api v1 exposes block read methods",
    "runtime api v1 exposes record hierarchy based on parent pointers",
    "runtime api v1 exposes Record Link Graph adjacency",
    "runtime api v1 exposes type composition dependencies",
    "runtime api v1 extracts block refs from nested field strings",
    "runtime api v1 open fails for invalid snapshot and preserves validation errors exactly",
    "runtime api v1 open returns session for valid dataset",
    "runtime api v1 raw markdown bytes preserve original bytes (no newline normalization)",
    "saves and loads an active dataset using IndexedDbStore",
    "saves and loads the active dataset",
    "sets retryAfterSeconds from Retry-After header on 429",
    "sets retryAfterSeconds from x-ratelimit-reset when remaining is 0 (403)",
    "short-circuits when validation fails",
    "shows a placeholder when no type body is present",
    "shows the selected type body content",
    "sorts paths lexicographically",
    "stores and retrieves values",
    "strips a single top-level folder in GitHub-style zips",
    "summarizes ignored files and dropped blocks with samples",
    "throws a not-implemented error",
    "throws a structured error when plugin bundles are missing",
    "throws a structured error when plugin bundles are missing from the repo tree",
    "throws a structured error when the GitHub tree listing is truncated",
    "throws deterministically on normalized-path collisions regardless of Map order",
    "throws on zip path normalization collisions",
    "throws when a plugin manifest cannot be parsed",
    "throws when a plugin manifest declares bundle files that are missing",
    "throws when binaryFiles is not a string array",
    "throws when declared paths are not safe relative paths",
    "throws when entry is not a string",
    "throws when files is not a string array",
    "throws when IndexedDB is unavailable",
    "TYPE-004 + VAL-005: fieldDefs map enforces required=true only",
    "wraps fetch rejections as ImportError",
    "zip snapshot roundtrips files"
  ]
}
